核心技术依赖 quic-go 的「同一 UDPConn 复用 QUIC + 裸 UDP」机制成立，已经在之前的评审中确认。

对探测*NAT映射生存期*的设计，与传统的实现方式不同：服务器简单回应即可，延时节奏完全由客户端掌控。这样更灵活，客户端可根据自身需求，自由控制探测精度（而与服务器无关）。

文档中部分功能属于应用层逻辑，不由协议层实现。之所以在协议文档中描述，是为了让整个逻辑完整畅通。协议层与应用层的区分详见 `docs/decision/DEC-0001-layer-boundary.md`。