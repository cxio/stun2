# NAT 类型/层级探测

客户端首先需要连入STUN服务网络，获取多个服务节点的联系信息，其中包含建立安全 QUIC 所需的如 `SPKI`、`ECH` 公钥等。
详见 [cxio/p2p 项目](https://github.com/cxio/p2p)。

然后客户端就可以与这些服务节点建立连接，并请求探测服务。

> **技术栈：**
> 客户端与服务器间的通信采用 *QUIC 安全连接 & 底层 UDP Socket 复用* 模式，其中 QUIC 用于安全传输，底层裸 UDP 用于 NAT 映射探测。
> Go 语言实现，使用 quic-go 库，参考 `quic.Transport{Conn: udpConn}`, `Transport.ReadNonQUICPacket()` 等用法，外部传入 `net.UDPConn`。


## 准备

节点首先应当确认自己是否位于 UDP 被封禁的网域：尝试与服务节点建立 QUIC 连接，如果连接成功，则进入下一步，否则继续尝试。如果多次尝试都失败，那大概率 UDP 被封禁（`UDP Blocked`），NAT 探测无意义。

> **提示：**
> 如果已经与服务器建立TCP连接（确定在线），可直接请求其UDP监听地址并尝试连接。
> 因为网络分享的服务器节点信息并不完全可用（还有毒IP），所以该选项有更高的优先级。


## 预探测（STUN:Addr, Step.0）

当确认自己的 UDP 链路可用后，客户端向多个服务节点（可配置，如*4*台）请求获取自身 UDP 地址（`STUN:Addr`），检查返回结果：

> **注：**
> `STUN:Addr` 请求返回的是客户端的地址二元组（`IP:Port`），下面的地址即指此。

- 如果多个服务器返回的客户端地址都不同，则确定为 `Sym-Like`，结束探测。
- 如果多个服务器返回的客户端地址有部分不同，也视为 `Sym-Like`，因为相同的部分可能只是网关的优化。结束探测。
- 如果所有服务器返回的客户端地址都相同，即视为 `非 Sym-Like`，进入下一步……

然后与本地地址（LocalAddr，含端口）对比：

- 相同（Y）：`Open Internet` | `UDP Firewall`，待进一步确认。
- 不同（N）：待正式探测，进一步判断……


## 正式探测（STUN:Cone）

客户端向任意一台服务节点请求 NAT 类型探测服务（`STUN:Cone`）。注意此时为 QUIC 安全连接。

> **提示：**
> 考虑可用性和可靠性，本流程可以尝试多次（如 `2~3` 次）。


### 客户端（Step.1）

客户端的 `STUN:Cone` 请求中包含如下数据：

1. Key32：会话密钥（32字节），用于构建会话标识（SN）。
2. Exclist：排除清单，包含在预探测期曾经连接过的目标地址。

> **解释：**
> `NewHost` 消息需要一个未曾与客户端连接过的新IP，因此需要排除清单（`Exclist`）。

服务端收到信息后，双方即可关闭 QUIC 安全连接，回退到该 QUIC 连接的底层普通 UDP 链路上通讯。


### 会话标识

会话标识（SN）是一个有着内在构造规则的标识，用于识别每一次的 NAT 探测消息。

**SN**的构造规则如下（伪代码）：

```go
// Rnd16: 16字节随机序列。
// Rnd16[0]: 首字节高两位置零，标识非 QUIC 数据包。
// Rnd16[0]: 首字节的低1位用于标记消息发送源（服务端）：
// - 0: NewPort 同机新端口发送
// - 1: NewHost 新主机发送

// 客户端：高2位清空。
Rnd16[0] = Rnd16[0] & 0x3F

// 服务端：高两位清空&低1为标记。
// from: NewPort | NewHost
Rnd16[0] = Rnd16[0] & 0x3E | from

// 客户端&服务端共用。
// TmpN: 变长随机字节序列，用于隐藏 SN 的长度特征。长度：464 ~ 1024
// SN 的有效部分为前段 16 + 32 = 48 字节。
SN = Rnd16 + HMAC_SHA256(Key32, Rnd16) + TmpN
```


### 服务器（Step.2）

服务端获取客户端发来的信息，然后执行如下操作：

- `NewPort`: 用一个新的随机端口向客户端发送消息，数据为即时构造的 SN。
- `NewHost`: 从本机的另一个新IP或请求另一个服务器向客户端发送消息，数据 SN 即时构造。


#### 受托服务器

如果是请求另一个服务节点协助执行 `NewHost`（委托发包），会向其提供必要的客户端信息（`Notice`）：

- 客户端公网映射地址（`IP:Port`）。
- 客户端的会话密钥封装 `SHA256( Key32 )`。

不会向受托服务器传递原始的客户端会话密钥，因为不可信的对端可能伪装成客户端向自己发确认包，从而终止自己向客户端的冗余发包。

**受托配合**：受托服务器会执行 `NewHost` 操作：向目标客户端发送纯 UDP 消息，消息内容为相同规则即时构造的 `SN`。

> **注意：**
> 如果是源服务器从自己本机的一个新IP发送 `NewHost`，也需要用封装之后的会话密钥构造 SN。
> 因为客户端只能借助于 `NewHost` 标识来判断会话密钥的采用值。


#### 源服务器安全

密钥封装是源服务器对受托服务器的不可信防范，但源服务器自身也不是确定可信的。受托服务器需要对源服务器进行安全防范，以避免恶意源节点借助于受托服务器制造 DDoS 攻击。

在P2P的环境下，自由连接的服务器节点间没有可信保证，设计一种可信机制会很复杂。因此这里我们使用**工作量代价**来进行 DDoS 防范，算法为 `Equi-X` PoW 哈希认证。

源服务器委托另一台服务器协助发送 NewHost 探测时，会有一次交互：

1. 源服务器向受托服务器发送 NewHost 协助请求。
2. 如果受托服务器接受请求，会返回一个挑战值 `challengeSeed`，32字节随机序列。
3. 源服务器执行运算：`solution, nonce := equix.Solve(challengeSeed)` 找到解，提交。
4. 受托服务器验证解，如果正确，即执行协助。

> **解释：**
> STUN 公共服务网络是一个P2P网络，大量的服务节点会使得每一个服务器的工作量并不大。
> 因此 NewHost 的工作量负担不应当是一个效率问题。
>
> 另外，两台服务器之间有一次交互，比没有交互直接委托更能保证受托服务器的协助意愿（有效性问题）。


#### 消息发送

服务器通过普通的裸 UDP 连接发送消息，需要实现冗余发送逻辑。消息的发送频率参考 RFC3489 协议：

初始间隔从 100ms开始，逐次加倍直到收到回复或超时。

间隔时间（ms）：100, 200, 400, 800, 1600, 1600, 1600, 1600, 1600 结束。
累计时长（ms）：100, 300, 700, 1500, 3100, 4700, 6300, 7900, 9500 超时。

> **注意：**
> 每一次发送的 SN 都是即时新建的，以避免链路中间件的优化合并或丢弃。
> 服务器收到客户端的回应后，即可停止冗余发送。


### 客户端（Step.3）

客户端读取收到的消息，提取前48字节以及发送源（`NewPort`|`NewHost`），根据规则核验 SN 是否有效。

> **注意：**
> 如果是 NewHost 消息，HMAC 的密钥应为：`SHA256( Key32 )`。

如果核验通过，客户端会即时回应，通知服务端停止冗余发送。回应的消息同样遵循 SN 构造规则。


#### 服务端：适时终止

对客户端回应的消息，服务器也需要进行验证，同时还应当确认不是自己发出的SN（重放攻击），避免误停止。

> **实现：**
> 服务器核实是否为自己发出的 SN 可简单地暂存 Rnd16 并对比即可。


### 综合判断（Step.4）

客户端根据 `NewPort` 和 `NewHost` 两个连入的情况，综合判断自身 NAT 类型或所在网域。

**NewPort**:
- 收到 => `RC | FullC`。**注**：`FullC` 在 NewHost 段确认&覆盖。
- 超时 => `P-RC | Sym-Like` => `P-RC`。**注**：`Sym-Like` 已在预探测中确认并排除。

**NewHost**:
- 收到 + 预探测.N => `FullC`。
- 收到 + 预探测.Y => `Open Internet`，即 `Public`。
- 超时 + 预探测.Y => `UDP Firewall` 网域。

> **提示：**
> 只要 NewHost 能收到消息，NewPort 的超时可以无需理会。


### 附：判断矩阵

| NewPort 收到 | NewHost 收到 | 与本地地址相同 | 结果 |
|:---:|:---:|:---:|------|
| - | Y | N | Full Cone |
| Y | N | N | RC |
| N | N | N | P-RC |
| - | Y | Y | Open Internet（Public） |
| - | N | Y | UDP Firewall |

**说明：**
- `N` 表示否定（No）。
- `Y` 表示肯定（Yes）。
- `-` 表示无需考虑，N/Y 皆可。


### 附：客户端超时

- **对齐时钟**：客户端在发出 STUN:Cone 请求后，其等待超时的计时器应略大于服务端的最大累计时长，此设置为**10**秒。
- **静默处理**：如果在 10 秒内客户端一个包都没收到，即可断定该探测项（NewPort 或 NewHost）为失败。


## 探测图示

```
                          +------------------+        NewHost Request
                          |      Serv.1      | ---------------------+
                          +------------------+                      |
                            |           /| |                        |
                            |            | |                        |
                            |          1)| |                        V
+--------+                  |   STUN:Cone| |                   +--------+
| Serv.0 |                  |            | |                   | Serv.2 |
+--------+                  |            | |                   +--------+
    |                       |            | | 2)                     |
    |                    0) |            | | NewPort                | 2)
    | 0)             Addr.1 |            | |                        | NewHost
    | Addr.0                |            | |                        |
    V                       V            | V                        V
+-------------------------------------------------------------------------+
|            LocalAddr                 Received?               Received?  |
|                                          3)                      3)     |
|  [Client]                                                               |
+-------------------------------------------------------------------------+


0)
Addr.0 != Addr.1   --> Sym-Like. END.
Addr.0 == Addr.1/
    Addr.1 == LocalAddr (Y)   --> Open Internet | UDP Firewall
    Addr.1 != LocalAddr (N)   --> (Next Step...)

4)
NewPort: Received?
    Yes   --> RC | FullC
    No    --> P-RC
NewHost: Received?
    Yes & 1.N   --> FullC
    Yes & 1.Y   --> Open Internet (Public)
    No  & 1.Y   --> UDP Firewall
```


### 附：传统NAT类型发现流程（RFC3489）

```
                        +--------+
                        |  Test  |
                        |   I    |
                        +--------+
                             |
                             |
                             V
                            /\              /\
                         N /  \ Y          /  \ Y             +--------+
          UDP     <-------/Resp\--------->/ IP \------------->|  Test  |
          Blocked         \ ?  /          \Same/              |   II   |
                           \  /            \? /               +--------+
                            \/              \/                    |
                                             | N                  |
                                             |                    V
                                             V                    /\
                                         +--------+  Sym.      N /  \
                                         |  Test  |  UDP    <---/Resp\
                                         |   II   |  Firewall   \ ?  /
                                         +--------+              \  /
                                             |                    \/
                                             V                     |Y
                  /\                         /\                    |
   Symmetric  N  /  \       +--------+   N  /  \                   V
      NAT  <--- / IP \<-----|  Test  |<--- /Resp\               Open
                \Same/      |   I    |     \ ?  /               Internet
                 \? /       +--------+      \  /
                  \/                         \/
                  |                           |Y
                  |                           |
                  |                           V
                  |                           Full
                  |                           Cone
                  V              /\
              +--------+        /  \ Y
              |  Test  |------>/Resp\---->Restricted
              |   III  |       \ ?  /
              +--------+        \  /
                                 \/
                                  |N
                                  |       Port
                                  +------>Restricted
```

文档：https://datatracker.ietf.org/doc/html/rfc3489#section-10.2
